{
  "name": "add-subtitles-to-video-root",
  "version": "1.0.0",
  "description": "Local AI Subtitle Studio Full Stack Application",
  "scripts": {
    "start": "docker compose up --build",
    "stop": "docker compose down",
    "clean": "docker compose down --volumes --remove-orphans"
  }
}
